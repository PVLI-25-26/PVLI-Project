/**
 * @typedef {Object} Movement
 * @property {function} update - Updates the movement each frame.
 * @property {number} speed - Movement speed in pixels per second.
 * @property {{x: number, y: number}} direction - Current movement direction.
 * @property {{x: number, y: number}} velocity - Current velocity vector.
 */
